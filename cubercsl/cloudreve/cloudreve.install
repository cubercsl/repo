post_install() {
    cat<<MSG
You need to run it manually once to initialize the database and generate the admin password.

  # su cloudreve -s /bin/bash -c cloudreve

After that it can be run with systemd.

  # systemctl start cloudreve.service

MSG
}
